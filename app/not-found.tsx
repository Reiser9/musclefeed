import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Muscle Feed - 404',
};

const NotFound = () => {
    return <div>Страница не найдена</div>;
};

export default NotFound;
